mod calculate_the_median;
//중앙값 계산
//숫자 목록의 계산
//홀수개의 요소가 있다.
fn main() {
    let answer = median(vec![1.0, 2.0, 5.0]);

    println!("median([1,2,5])={:?}", answer)
}
fn median(a: Vec<f32>) -> Option<f32> {
    //todo!메인코드 동작은 프로그램을 충돌
    todo!();
}
